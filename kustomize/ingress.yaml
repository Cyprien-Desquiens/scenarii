---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ws-cpt-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ws-cpt
                port:
                  name: web